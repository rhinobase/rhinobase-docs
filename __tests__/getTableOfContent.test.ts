import getTableOfContent from "../utils/getTableOfContent";

describe("Items for the table of content", () => {
  test("just pass it", () => {
    expect(true).toBe(true);
  });

  // test("No content", () => {
  //   const content = "";
  //   expect(getTableOfContent(content)).toEqual([]);
  // });
  // test("Content with no headings", () => {
  //   const content = "This is a simple simple string";
  //   expect(getTableOfContent(content)).toEqual([]);
  // });
});
